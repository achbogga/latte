{
  "name": "@achbogga/latte-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsup src/server.ts --format esm --dts --tsconfig ../../tsconfig.base.json",
    "dev": "tsx watch src/server.ts",
    "lint": "eslint src --ext .ts",
    "typecheck": "tsc --project ../../tsconfig.base.json --noEmit"
  },
  "dependencies": {
    "@achbogga/latte-core": "workspace:*",
    "fastify": "^5.6.1",
    "zod": "^4.1.12"
  }
}
